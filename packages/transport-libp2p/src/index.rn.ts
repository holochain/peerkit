/**
 * React Native entry for the {@link "@peerkit/transport-libp2p"} facade.
 *
 * Resolved by Metro when the `react-native` condition is active.
 */
export * from "@peerkit/transport-libp2p-react-native";
