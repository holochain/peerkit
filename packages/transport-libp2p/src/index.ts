/**
 * Default entry for the {@link "@peerkit/transport-libp2p"} facade.
 *
 * Resolved when conditional `exports` does not match `react-native` — i.e.
 * Node.js and any other non-RN runtime.
 */
export * from "@peerkit/transport-libp2p-nodejs";
