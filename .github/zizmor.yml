rules:
  unpinned-uses:
    config:
      policies:
        "actions/*": ref-pin
        "*": hash-pin
  artipacked:
    # checkout token is reused to push the release tag
    ignore:
      - release-publish.yml
